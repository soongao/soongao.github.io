# 关键模块
> 项目：`uri_task_control_panel`。本页由当前 Codex 会话基于 GitNexus 索引元信息和 `.ua/knowledge-graph.json` 生成。
> GitNexus clusters：33本页按顶层目录组织模块视图，适合快速定位职责边界。

## internal

该目录包含 24 个文件级节点。类型分布：file: 24。

- `internal/api/handlers_test.go`：internal/api/handlers_test.go 是 go code 文件，包含 9 个函数。
- `internal/api/handlers.go`：internal/api/handlers.go 是 go code 文件，包含 27 个函数、1 个类型/类。
- `internal/api/response.go`：internal/api/response.go 是 go code 文件，包含 2 个函数、1 个类型/类。
- `internal/barrier/barrier_test.go`：internal/barrier/barrier_test.go 是 go code 文件，包含 4 个函数、1 个类型/类。
- `internal/barrier/barrier.go`：internal/barrier/barrier.go 是 go code 文件，包含 4 个函数、2 个类型/类。
- `internal/collector/collector_test.go`：internal/collector/collector_test.go 是 go code 文件，包含 4 个函数。
- `internal/collector/collector.go`：internal/collector/collector.go 是 go code 文件，包含 3 个函数、1 个类型/类。
- `internal/config/config.go`：internal/config/config.go 是 go code 文件，包含 4 个函数、9 个类型/类。
- `internal/finalizer/finalizer_test.go`：internal/finalizer/finalizer_test.go 是 go code 文件，包含 4 个函数、1 个类型/类。
- `internal/finalizer/finalizer.go`：internal/finalizer/finalizer.go 是 go code 文件，包含 3 个函数、2 个类型/类。
- `internal/job/file_scanner_test.go`：internal/job/file_scanner_test.go 是 go code 文件，包含 5 个函数、2 个类型/类。
- `internal/job/file_scanner.go`：internal/job/file_scanner.go 是 go code 文件，包含 11 个函数、6 个类型/类。
- `internal/job/manager_test.go`：internal/job/manager_test.go 是 go code 文件，包含 25 个函数、4 个类型/类。
- `internal/job/manager.go`：internal/job/manager.go 是 go code 文件，包含 29 个函数、4 个类型/类。
- `internal/scheduler/lambda_launcher_test.go`：internal/scheduler/lambda_launcher_test.go 是 go code 文件，包含 1 个函数。
- `internal/scheduler/lambda_launcher.go`：internal/scheduler/lambda_launcher.go 是 go code 文件，包含 5 个函数、5 个类型/类。

## .

该目录包含 4 个文件级节点。类型分布：config: 2，document: 1，file: 1。

- `build.sh`：build.sh 是 shell script 文件。
- `go.mod`：go.mod 是 mod config 文件。
- `go.sum`：go.sum 是 sum config 文件。
- `README.md`：README.md 是 markdown docs 文件。

## .ua

该目录包含 2 个文件级节点。类型分布：config: 1，file: 1。

- `.ua/.understandignore`：.ua/.understandignore 是 unknown code 文件。
- `.ua/config.json`：.ua/config.json 是 json config 文件。

## conf

该目录包含 2 个文件级节点。类型分布：config: 2。

- `conf/base.yml`：conf/base.yml 是 yaml config 文件。
- `conf/hertz.config.yaml`：conf/hertz.config.yaml 是 yaml config 文件。

## api

该目录包含 1 个文件级节点。类型分布：config: 1。

- `api/swagger.yaml`：api/swagger.yaml 是 yaml config 文件。

## cmd

该目录包含 1 个文件级节点。类型分布：file: 1。

- `cmd/main.go`：cmd/main.go 是 go code 文件，包含 1 个函数。

## docs

该目录包含 1 个文件级节点。类型分布：document: 1。

- `docs/integration.md`：docs/integration.md 是 markdown docs 文件。

## examples

该目录包含 1 个文件级节点。类型分布：file: 1。

- `examples/hertz_client/main.go`：examples/hertz_client/main.go 是 go code 文件，包含 9 个函数、1 个类型/类。

## script

该目录包含 1 个文件级节点。类型分布：file: 1。

- `script/bootstrap.sh`：script/bootstrap.sh 是 shell script 文件。
